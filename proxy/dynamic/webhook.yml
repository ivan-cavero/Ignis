http:
  routers:
    webhook:
      rule: "Host(`webhook.ivancavero.com`)"
      entryPoints:
        - "websecure"
      service: "webhook"
      tls:
        certResolver: "letsencrypt"

  services:
    webhook:
      loadBalancer:
        servers:
          - url: "http://172.17.0.1:3333"  # IP host Docker
        passHostHeader: true
